# 移交变更说明（给接收方 AI 读）

> 本文件面向**接收方的 AI 助手**：说明本批交付了什么、相对上一版变了什么、怎么直接用起来。
> 仓库定位见 `README.md`，首次落地步骤见 `BOOTSTRAP.md`（**先读 BOOTSTRAP 再动手**）。
> 交付形态：可 `git clone` 直接复建的悟空能力副本（技能 + 知识库 + 工作流 SOP + 模板）。
> 本仓只含"能力与知识"，**不含工作档案数据**——历史评估/评审/质检档案走离线包（见 `data-manifest.md`）。

---

## 本次交付范围：2026-07-27 → 2026-08-17

**接收方上一次拿到的是 2026-07-27 批次（commit `d5c8b72`），本文件覆盖此后全部变更。**
其间共 3 次发布，合计 44 个文件变更（+1633 / −209 行）。
自行核对：`git log --oneline d5c8b72..HEAD`、`git diff --stat d5c8b72 HEAD`。

| 类别 | 变了什么 | 要不要动手 |
|---|---|---|
| **紧箍咒（企微质检）** | 新增 Step 3 合规预警标注、12 类标签规则库、`label2xlsx.py`、七维扩至 42 条子规则 | **要**：新能力，见第一节 |
| **照妖镜（活动评审）** | docx 格式校验三处踩坑订正（假 PASS 修复） | **要**：见第二节 |
| **判定口径** | 六维规则库新增老年客群/高风险产品双重筛查、定级判例 6；事件库补客户服务两节 | **要**：评估结论会因此改变，见第三节 |
| **知识素材** | 8 个活动卡、1 个案例、8 篇 daily | 不用：读即可 |
| **基础设施** | `index_lint.ps1` 瘦身为三项；`memory/海马体.md` 升器官 v1 | 视情况：见第五节 |
| **模板** | 4 个 office 原件移出本仓改走离线包 | 需要原件的问移交人要 |

---

## 一、紧箍咒（`anzhi-jinguzhou`）企微聊天质检 —— 新增「合规预警标注」能力

**本批最大的变更，移交人特别要你关注的一项。**

**这个技能是干什么的**：把企微导出的客户经理—客户聊天记录，逐行判定有没有合规问题，产出可直接报送的标注表与体检报告。

### 新增了什么

1. **新增 Step 3「合规预警标注」，产出第 4 件产物 `04-合规预警标注表.xlsx`**
   - 判定依据是新增规则库 `references/合规预警标签规则.md`（197 行，转自行内规则表 v2.0），含 **12 类合规预警标签**：
     ①保本保息或收益承诺 ②宣传预期收益率或未来收益 ③风险提示不到位或弱化风险 ④未宣告业绩比较基准、过往业绩不代表未来收益 ⑤未说明代销性质 ⑥混淆产品性质或不当类比 ⑦极端或夸大性表述 ⑧饥饿营销 ⑨不当利益诱导营销 ⑩贬低同业产品或机构 ⑪利用监管、政府或公共机构公信力营销 ⑫骚扰客户
   - 规则库 §一 自带**判定纪律**（优先级 1—6、单一主标签原则、可并列情形、引用消息归属、可信度五级、一般不预警清单），**是唯一判定依据，不要靠常识自由发挥**。
   - **全量逐行、判否行也要出**（训练件口径，不是只输出问题行），缺行会被闸门拦下。
   - **顺序有讲究**：标注排在七维扫描**之前**，是为了让标注判定不被质检结论锚定，别为了省事调换。
2. **新增合成脚本 `scripts/label2xlsx.py`**（167 行）+ 测试 `scripts/tests/test_label2xlsx.py`
   - 标注结果先逐行写 `04-标注.jsonl`，再由脚本合成 xlsx；脚本内置三道闸门（漏行 / 幻觉行号 / 非法标签）。
   - ⚠️ **闸门报错要回去修 JSONL 重跑，不许手改 xlsx**——手改会在下次重跑时被覆盖，且绕过校验。
   - JSONL 本身即断点：续跑读已有行号集合，从缺失的最小行号所在批次继续，只追加不覆盖。
3. **七维规则库扩充**：`references/质检维度规则库.md` 维度①新增第 8、9 条子规则（不当利益诱导、饥饿营销），现为**七维共 42 条子规则**。
   ⚠️ 对外表述一律为"七维规则框架，其中维度①新增两条子规则"，**不要写成"七维扩九维"**（维度数没变，变的是子规则条数）。
4. **原步骤顺延**：原 Step 3—7 顺延为 **Step 4—8**（`03-体检报告.md` 同步扩出第七节）。
   如果你手上有旧版笔记写着"Step 3 七维扫描""Step 7 复核回填"，**以现版 SKILL.md 为准**（Step 4 七维、Step 5 审计、Step 7 沉淀回填、Step 8 复核回填）。
5. **脚本健壮性修复**：`parse_chats.py`（sheet 按 A1 内容自选，定位不到会话 sheet 时报错列名单而不是猜）、`md2xlsx.py`（"待人工确认"段遇 `###` 小节即止，防抽检建议被误吞）。

### 跑一遍的完整链路（触发词：`聊天质检` 或 `紧箍咒`）

```
企微导出 xlsx 放入 work/事后质检/原始/
  → Step 0 收料确认（批次名、格式校验、抽看前 20 行看有无未脱敏实名/手机号/卡号）
  → Step 1 机械解析：
       python .claude/skills/anzhi-jinguzhou/scripts/parse_chats.py <xlsx绝对路径> <批次目录绝对路径>
       产出 01-解析.txt + 01-原文映射.jsonl
  → Step 2 角色识别（谁是客户经理；强信号=产品链接 URL 含 refereeId=，必为员工侧发出）
  → Step 3 合规预警标注：分批 20—30 行逐行判定写 04-标注.jsonl，再
       python .claude/skills/anzhi-jinguzhou/scripts/label2xlsx.py <批次目录>
       产出 04-合规预警标注表.xlsx
  → Step 4 七维规则扫描（42 条子规则，每条命中必须引用条目及出处）
  → Step 5 审计关注点专项抽取（公式 A ∧ 非b ∧ c ∧ 非D，独立于 Step 4 单独跑）
  → Step 6 报告产出：02-审计关注点清单.md（一等产出）+ 03-体检报告.md
  → Step 7 沉淀回填 / Step 8 复核回填（触发词：质检复核）
```

### 三条硬约束（照做，别绕）

- **脱敏责任在数据源头**：04 标注表不做二次脱敏。Step 0 抽看时发现未脱敏的客户实名/手机号/卡号，**停下来报人**，不要静默往下跑。
- **判定不了就标"待人工确认"**，不硬判——本技能原则是"AI 判定、人拍板"，拿不准的进待确认，不静默吞掉。
- **测试样例的 B 列是人工标注答案，判定时不得读取**（生产数据没有 B 列，读了等于作弊并污染验证）。

---

## 二、照妖镜（`anzhi-huodongpingshen`）活动评审 —— docx 格式校验三处订正

评审报告 docx 的表格缩进校验有三个坑，2026-08-05 实测踩中后订正，**照旧写法会得到假 PASS**：

1. **必须用正则 `<w:tblInd[^>]*w:w="0"`，不可用固定串 `<w:tblInd w:w="0"`**——docx 库 9.7.1 实际输出是 `<w:tblInd w:type="dxa" w:w="0"/>`，`w:type` 属性在前，按固定串检索恒为 0 命中，会把完全合格的文档判成不合格。属性顺序由库版本决定，只认属性值不认顺序。
2. **解包方式**：Windows 上 `Expand-Archive` 拒收 `.docx` 扩展名（只认 `.zip`），直接用会报错。改用 .NET `[System.IO.Compression.ZipFile]::OpenRead()` 读 zip 内 `word/document.xml`，无需落盘。
3. **判定式写法**：必须写 `($tbl -gt 0) -and ($tbl -eq $ind)`。只写 `$tbl -eq $ind` 时，解包失败导致两边都为空会得出 `0 -eq 0` 的**假 PASS**。

---

## 三、判定口径变更（会改变评估结论，务必读）

### 1. 老年客群 / 高风险产品双重筛查（六维规则库 `04-事项合理性.md`，每次评估先过一遍）

先问两件事：①**是否涉及老年客群**（材料是否按年龄筛选、有无现场营销/代客操作/需客户签字确认环节）；②**是否涉及高风险产品或理财推荐**（是否指定/推介具体理财产品，而非泛化的"资产提升"类中性任务）。

- 两者均不涉及 → 判无风险，不必展开深挖。
- 涉及高风险产品/理财推荐（不论是否已知涉老）→ **要求处室在客群圈选规则中明确排除老年客群，或对老年客户只投放低风险权益/任务**（不得让老年客户与其他客群共用同一套高风险产品推介规则），并按事件库 #13"权益与非必要业务捆绑诱导办理风险"三要素叠加判据（业务捆绑/代客操作/适老客群）逐项核查。

> 来源：2026-07-19 某分行现场营销代客开立证券账户事件（老年、无读写能力客户 + 代客签名 + 证券账户捆绑）教训固化。

### 2. 定级判例 6（六维规则库 `05-评估准确性.md`）：新增触达渠道继承被引流对象的当期状态

评审对象若不是活动本身、而是**指向某个已有活动的新增引流物料/话术**（企微/短信/Push 新增提及某活动），即使物料内容本身无违规、孤立评审会落四级，**舆论背景维仍应继承被引流对象的当期状态**——新增引流渠道的风险不独立于被引流对象存在，在风险未回落的窗口内扩大曝光等于主动加码传播，按孰高原则取三级。

**通用做法**：评审"引流类/新增触达渠道类"材料时，不能只审内容本身，**还要核查引流对象当前是否处于既往评估确认的敏感窗口**。适用范围不限于活动评审，声誉风险评估中遇到"为存量活动新增推送渠道/物料"的申请同样适用。

### 3. 声誉风险事件库补充

- `03-客户服务.md`：新增「远程经营中心（补充·3.1）」与「3.2 对客公告与批量资产处置」两节（含核心隐患要点与前置预防举措）。
- `01-数字营销活动.md`：条目增补。
- 配套新增案例 `knowledge/cases/20260731-批量资产处置名单未核查身故客户.md`。

---

## 四、知识素材增量（读即可，无需动手）

- **活动方案库新增 8 个活动卡**：薪动有礼、动态令牌认证引擎系统暂停服务、保管箱逾期清理公告、支付有礼月月刷、理财外呼话术、一周市场观察、基客积点兑换（另有 2 个既有卡片更新），`INDEX.md` 同步。
- **工作日志**：新增 2026-07-28 / 07-30 / 07-31 / 08-03 / 08-04 / 08-05 / 08-11 共 8 篇 daily（`knowledge/daily/`），是上述案例与判例的原始素材。

---

## 五、基础设施与模板变更

- **`scripts/index_lint.ps1` 瘦身为三项检查**：原第 4 项"记忆结构体检"退役（避免两套解析器口径漂移），检查 1—3 不变，测试同步收缩。
- **`memory/海马体.md` 升级为"器官 v1"结构**：封闭类目表 + 每条必填出口 + 水位自查。这是记忆文件的结构规范，若你要复用记忆机制，照此结构写。
- **4 个 office 模板原件移出本仓**（声誉风险事前评估表 xlsx、广告宣传规范审核表 xlsx、消保审查表 xlsx、回撤预案模板 docx）→ 改走离线数据包通道，本仓只留 `templates/originals/.gitkeep` 占位。**需要模板原件的，问移交人要离线包**，不是漏发。
- `templates/活动卡片模板.md` 字段枚举小幅更新。

---

## 接收方怎么用起来（首次落地）

1. `git clone` 本仓到本地工作目录。
2. **先读 `BOOTSTRAP.md`**——落地步骤的唯一权威（依赖、目录初始化、需要人工补的空位）。
3. 读 `CLAUDE.md`（工作规范）+ `soul.md`（角色定位）+ `WORKSPACE.md`（多 agent 协作约定与技能清单）。
   非 Claude 的 AI（Kimi / Codex 等）走 `AGENTS.md` 入口，技能发现协议见 `WORKSPACE.md`。
4. 技能在 `.claude/skills/<技能名>/SKILL.md`，**每个技能的 SKILL.md 就是它的完整操作手册**，按里面的 Step 执行即可，不需要额外文档。
5. 数据目录（`work/`、`internal-comms/`、`knowledge-raw/`）本仓只给空占位；历史档案走离线包，用 `data-manifest.md` 的批次台账核对完整性。

## 已知的未随交内容（不是漏发）

- **数据档案**：历史评估/评审/质检工作档案，体量大且含更多内部信息 → 离线 zip 人工递送。
- **office 模板原件**：同上，走离线包。
- **部分内部专用技能**：与移交人个人邮箱/服务器/跨实例协作强绑定的编排类技能（邮件监听、跨实例通信、对外移交维护等）不随交——它们依赖移交人本机环境，对接收方无意义。本仓 12 个技能即为可独立复用的完整集合。
