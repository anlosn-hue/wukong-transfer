---
type: memory-index
# updated: <YYYY-MM-DD>
---

# 海马体

> 器官版本：v1（结构规范＝本模板；升级随移交仓同步。移交环境无自动体检，以下水位与压缩为**自查口径**）
> 水位阈值：<N> 字符（<YYYY-MM-DD> 用户定）——单条超限提示压缩，详情移入 worklog
> 压缩时点：<可判定事件名，如「会话结束时」>（<YYYY-MM-DD> 用户定）——不许写「适时」

> 跨会话短期暂存——**只收有出口的条目**。行为规则三条：
> ①完结即删不过夜（git 历史即归档）；②详情落归宿（worklog/工作档案），此处只留结论＋出口＋指针，不复述；③类目表封闭（见触发清单）——不属任何类当场分诊（backlog / 删 / 升格主题文件），分诊不了问用户。长期挂账一律 `memory/backlog.md`。

## 待处理条目

| 条目 | 类型 | 出口 | 来源/时间 |
|------|------|------|---------|
| （暂无） | — | — | — |

> 出口列写法：凡在等用户拍板的条目，出口以「待用户：」开头。

## 触发清单（封闭类目表——遇下列任一情况立即追加，不等用户发起）

| 类型 | 触发 | 出口 |
|------|------|------|
| 任务跨会话 | 任务未完成跨会话 | 一行进度＋下一步＋指针（详情写 worklog）；完结→提炼归档→删条 |
| 专业判断（定级判例） | 非显而易见的定级判断（含评估校正发现的审批链改级案） | 用户点头 → 迁 `六维规则库/05-评估准确性.md`「定级判例」节 → 删条 |
| 新排查要点 | 评审/评估中发现新隐患排查点 | 用户点头 → 迁 `声誉风险事件库/` 对应场景文件（+需要时 cases/，登记 INDEX）→ 删条 |
| 用户的纠正 | 用户纠正了业务/工具判断 | 固化进 memory 主题文件或对应规则/数据 → 删条 |
| 监管新动向 | 新法规/监管要求/同业处罚 | 用户分诊：事件库 / backlog（带到期日）/ 删 → 删条 |

> 本表**封闭**：可按业务增删改类目（改表须用户点头），但「表须封闭＋每类必有出口」是结构规则不可去。待处理表「类型」列取值须与本表首列一字不差。

**会话结束前必过一遍触发清单；完结条目当场删。**
